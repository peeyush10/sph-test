<?php

/**
 * Implements hook_theme().
 */
function qr_scanner_block_theme($existing, $type, $theme, $path){
    return [
    'block_qr_scanner_template' => [
      'variables' => ['image_url' => NULL],
    ],
  ];
}